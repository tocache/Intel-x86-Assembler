DATOS SEGMENT
  CADENA DB "Ingenieria de Computacion y Sistemas$"
  CADENA2 DB 13,10,"Organizacion y Arquitectura de PC$"
DATOS ENDS

CODIGO SEGMENT
ASSUME CS:CODIGO, DS:DATOS

INICIO:	MOV AX, DATOS
	MOV DS, AX
	MOV AH, 09H
	MOV DX, OFFSET CADENA
	INT 21H
	MOV DX, OFFSET CADENA2
	INT 21H

FIN:	MOV AH, 4CH
	INT 21H

	CODIGO ENDS

END INICIO
